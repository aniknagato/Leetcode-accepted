class Solution {
    public int[] sortedSquares(int[] A) {
        
        int l = A.length;
        
        for(int i=0;i<l;i++){
            A[i] = A[i]*A[i];
            
            
        }
        Arrays.sort(A);
        
        return A;
        
    }
}
